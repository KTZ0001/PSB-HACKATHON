# Aegis — continuous identity-trust scoring engine
# Python 3.11+ (developed/tested on 3.12)
#
# Pinned to compatible major versions. Do NOT add dependencies beyond this
# list without explicit approval (see build prompt: Tech Stack constraint).

pandas>=2.1,<3.0
# Upper bound matches scipy 1.13.1's requirement (numpy<2.3); a looser bound lets
# a fresh `pip install` pull a numpy that scipy 1.13.1 can't import.
numpy>=1.26,<2.3
# scipy pinned to a mature, widely-distributed build. Newer scipy wheels ship
# unsigned native DLLs that Windows Smart App Control blocks (no reputation in
# Microsoft's security graph); 1.13.1 is recognized and loads cleanly. Do not
# bump without re-verifying `import scipy.linalg` works under Smart App Control.
scipy==1.13.1
scikit-learn>=1.4,<2.0
xgboost>=2.0,<3.0
fastapi>=0.110,<1.0
uvicorn[standard]>=0.29,<1.0
pydantic>=2.6,<3.0
joblib>=1.3,<2.0
kaggle>=1.6
networkx>=3.2,<4.0
pytest>=8.0,<9.0
# Test-only: FastAPI's TestClient (starlette) requires httpx for HTTP-level API
# tests (tests/test_api.py). Not used in the runtime serving path.
httpx>=0.27,<1.0

# Demo script terminal formatting (Phase 5). Listed in the build prompt's
# Phase 5 ("e.g. using rich"). Pure-presentation dependency only.
rich>=13.7,<14.0
